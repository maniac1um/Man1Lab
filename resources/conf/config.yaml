defaults:
  - parser: default
  - analysis: default
  - discovery: default
  - execution_planning: default
  - planner: default
  - coder: default
  - runner: default
  - reviewer: default
  - reporter: default
  - workflow: default
  - llm: default
  - logging: default
  - tracking: default
  - _self_

workspace_root: var/workspace/tasks
outputs_dir: var/outputs
logs_dir: var/logs
prompts_dir: resources/prompts
paper_path: ${oc.env:PAPER_PATH,paper.pdf}
