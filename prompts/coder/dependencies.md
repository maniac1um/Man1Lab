Generate a Python requirements file listing packages needed for the reproduction project.

Follow the Shared generation context in the user message. Use the same framework and
python version as the context. Include every package that scripts and source modules
in the repository will import.

Include only package names and version constraints. One package per line.
