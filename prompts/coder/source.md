Generate a Python source module for the reproduction project.

Follow the Shared generation context in the user message. Use the framework, dataset,
and model described in the context. Export symbols that scripts can import.

Include docstrings and minimal runnable structure. Do not generate multiple files.
