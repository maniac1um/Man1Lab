Generate a YAML configuration file for the reproduction project.

Follow the Shared generation context in the user message. Use the same dataset,
optimizer, and training settings as the context. Match the schema expected by
scripts in the repository.

Include only configuration relevant to the target file path.
